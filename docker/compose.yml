version: "3.3"

services:
  server:
    image: nginx:alpine
    volumes:
      - ../public:/usr/share/nginx/html
      - ../nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - chip8
    ports:
      - 8000:80

networks:
  chip8:
